# Retro — 2026-05-15

**Branch:** `fix/node-gates-investigation`
**Scope:** 5 commits on this worktree (538074f → 49a5764)
**Mode:** headless (no decision/routing phases — pr-monitor bg context)

## Pattern narrative

Investigation-driven session: a node/npm absence on the runner exposed two gate-script bugs (tail masking exit codes; missing toolchain preflight). Work proceeded as a clean investigation → recommendation → fix → spec-formalization loop. No thrashing, no rollback commits, no user interrupts.

## Mechanical signals

- Transcripts examined: 4 (this session + 3 sibling)
- user_corrections: 0
- tool_failures: 0
- repeated_commands: 0
- stop_hook_blocked: false
- frustration_hits: 0
- allowlist_drift proposals: 29 (historical PR-fix pattern, **not** this session)

## What changed

| Commit | Kind | Surface |
|---|---|---|
| 538074f | feat | scripts/start-bg-spawn.py (--permission-mode pass-through) |
| 99f78c6 | docs | .investigation/ (node/npm absence + tail-masks-exit recommendation) |
| 43be4f6 | fix | gates skill (preflight toolchain + forbid `tail` without `pipefail`) |
| 15c66a6 | docs | .investigation/ (past-PR audit) |
| 49a5764 | fix | specs/dispatch-routing.md (AC-17 opt-in pass-through) |

## What worked

- **Investigation-first.** The two gate bugs (toolchain assumption, tail masking) were diagnosed before any code change. Recommendation doc landed first; fix mirrors the recommendation.
- **Mixed kinds in one session without thrash.** docs + fix + feat + spec — usually a smell, but here each commit advanced one coherent thread.
- **Spec follow-through.** AC-17 was formalized after the feature shipped, closing the spec/code gap in the same session rather than punting.

## Findings

No new findings from this session warrant routing. Mechanical signals are clean; no smell patterns triggered.

The 29 allowlist-drift proposals (in `.workflow/retro-findings.json`) are carried forward from historical post-/verify fixes across earlier PRs — they describe files that historically required follow-up fixes after `/verify` passed. They should be considered when next updating the shakedown allowlist but are not session-attributable.

## Carryover

`.retros/summary.json` has no `entries[]` array (schema uses `findings_by_category`); last_updated 2026-04-26, total_retros 14. No carryover findings to reconcile in this run.

## Meta

None surfaced.
